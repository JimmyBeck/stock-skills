# TESTING.md — 新用户验收测试单（小白剧本版）

模拟对象：小红书炒股用户——不会命令行、不懂代码，只会用自然语言和自己的 AI agent 对话。
测试方式：在一台干净机器上，由测试者（或测试 agent）**逐句照抄剧本**与 agent 对话，观察反应是否符合预期。

> 通过标准：剧本 A（主线）全部通过 = 可发布；剧本 B（刁难）每个场景 agent 都有合理应对 = 稳定。
> 任何问题记录：剧本编号、你说了什么、agent 实际做了什么、预期是什么。

## 剧本 A：主线（aha 之旅）

**A1. 安装**
说：`帮我安装这个 skill：<GitHub 地址>`
预期：agent 下载并把两个 skill 装进 skill 目录，回复"安装好了"，并简单说明能做什么。
判定：未要求你做任何手工操作即完成 = 通过。

**A2. 欢迎引导**
说：`这个 skill 能干嘛？`
预期：agent 用大白话介绍：每天自动收集股票数据（行情+MACD 15 个字段），存本地表格或在线文档，并主动邀请"给个股票代码试试"。
判定：没有技术黑话（不出现 JSON/config/registry 等词）= 通过。

**A3. 首次拉数（核心 aha 时刻）**
说：`拉一下五粮液最近3天的数据`
预期：
1. agent 先问你"数据想放哪里？"（本地表格/腾讯文档）
2. 你说`放本地就行`
3. agent 完成接入并拉取 3 天数据，**用表格把结果展示给你看**（日期/收盘/涨跌/换手率等 + MACD）
4. 告诉你文件存在哪、怎么每天自动更新
判定：被询问了存放位置 + 看到表格化的 3 天数据 = 通过。

**A4. 数据对得上**
操作：打开同花顺/雪球，对照表格里最近一天的收盘价、涨跌幅。
判定：与行情软件一致（小数点后两位内）= 通过。

**A5. 数据文件能打开**
操作：按 agent 给的路径，用 Excel 双击打开生成的 CSV。
判定：中文表头不乱码、数据整齐 = 通过。

**A6. 每天自动更新**
说：`给五粮液设置每天收盘后自动更新`
预期：agent 注册定时任务（告诉你每天几点跑），不需要你做任何事。
判定：定时任务存在且时间合理（收盘后，如 16 点）= 通过。

**A7. DIY 新字段**
说：`我还想收北向资金持股，帮我看看能不能弄到`
预期：agent 先和你确认口径（北向资金有几种说法），去调研数据源，**带着真实样本数据**回来给你确认；拿不到就如实说并给替代方案。
判定：没有瞎编数据源；有证据或如实说明 = 通过。

## 剧本 B：刁难场景（稳定性）

| # | 你说 | agent 应有的表现 |
|---|---|---|
| B1 | `拉一下五粮夜的数据`（错别字） | 识别为五粮液 000858，或拿不准时反问，不瞎接 |
| B2 | `拉 sh999999 的数据`（不存在） | 明确告知代码无效，不编造数据 |
| B3 | `拉 ST 退市股/长期停牌股` | 告知停牌/无数据现状，问你是否还要接入 |
| B4 | 周末说`拉今天的数据` | 说明今天非交易日，给最近交易日的数据或询问 |
| B5 | `拉腾讯控股的数据`（港股） | 告知港美股部分字段（换手率/市值/PE）暂无法自动获取，问你是否继续 |
| B6 | `帮我查主力情绪指数`（不存在的概念） | 说明没有现成数据，推荐可得的替代（如主力净流入），不硬造 |
| B7 | `我要市盈率`（不说哪种） | 告知默认是 TTM 口径，说明还有静态/动态可选，问你要哪种 |
| B8 | `茅台五粮液泸州老窖都给我接上`（一次多只） | 逐只接入或询问是否批量，不漏不混 |
| B9 | 拉完数据后说`这些数据存哪了？我要腾讯文档` | 能切换存储到腾讯文档（或说明需要什么信息） |
| B10 | 接入后马上说`再拉一遍昨天的` | 幂等跳过，不产生重复行 |
| B11 | （数据源故障模拟）东财接口挂了 | 自动切换备用源；都挂则留空+明确告警，不编数 |
| B12 | `把上个月的数据改了`（让你改历史） | 说明会重新核算并报告改动，不悄默声改 |
| B13 | 给个 Excel 表头说`照这个样子给我搞一份` | 解析表头 → 逐字段匹配（已有/同义/新字段），给对照表确认 |
| B14 | `这个准不准啊` | 说明多源交叉验证机制，可现场跑完整性核查给你看 |

**B 组判定**：无崩溃、无编造、有引导或明确报错 = 每个场景通过。

## 问题反馈格式

```
剧本编号：A3 / B7
我说了什么：
agent 实际做了什么：
我预期的：
环境：（agent 名称、操作系统、是否企业网络）
```

# 使用指南（小白版）

这份指南给**不会写代码**的你。你不需要敲任何命令——所有事都用大白话告诉你的 AI agent 就行。

## 你能得到什么

给一个股票代码，每天自动帮你收集这些数据，存成表格：

- **行情**：收盘价、涨跌额、涨跌幅、成交额、换手率、量比、振幅、总市值、流通市值、市盈率（TTM）
- **MACD 技术分析**：日线/周线/月线的 DIFF、DEA、MACD 柱、零轴状态、金叉死叉、动能方向
- **板块参照**：所属行业板块当天涨跌幅

数据存在你电脑的表格文件里（CSV 格式，Excel 双击就能打开），也可以存到腾讯文档。

## 第一次用（3 分钟）

对你的 agent 说：

> 帮我安装这个 skill：https://github.com/（项目地址）

装好后说：

> 拉一下五粮液最近3天的数据

agent 会问你想把数据放哪（第一次用建议直接说"放本地表格就行"），然后帮你接入、拉数据、展示结果。看到表格那一刻，你就已经会用了。

## 常用操作（照抄改字就行）

| 你想干什么 | 对 agent 说 |
|---|---|
| 再收一只股票 | "再帮我接入宁德时代 300750" |
| 补历史数据 | "帮我把茅台最近30天的数据补齐" |
| 每天自动更新 | "给茅台设置每天收盘后自动更新" |
| 检查数据有没有问题 | "检查一下茅台的数据完整性" |
| 想要新字段 | "我还想收集北向资金持股，帮我看看能不能拿到" |
| 换存放位置 | "把数据改存到腾讯文档" |
| 某个字段不想要了 | "茅台的数据里把量比关掉" |

## 关于新字段（进阶）

内置的 15 个字段只是开始。想收别的数据（北向资金、融资融券、股东人数……），直接跟 agent 说你要什么：

1. agent 会跟你确认口径（比如"北向资金"有好几种理解，它会给你选项）
2. 它会去调研哪个数据源能拿到、靠不靠谱，**拿到真实样本证据**再给你看
3. 你确认后，它把新字段接进你的表格，从当天开始每天自动收

如果某个数据确实没有可靠来源，它会如实告诉你，并推荐能拿到的替代品——**它不会编数据**。

## 常见问题

**Q：为什么历史日期的市值、量比、市盈率是空的？**
A：这几个数据只有实时行情，查不到历史值，所有数据商都一样。从接入那天起每天都会正常记录，历史部分确实补不了。

**Q：为什么板块涨跌幅那一列是空的？**
A：这一列需要知道你的股票属于哪个行业板块（配置里的 sector 参数）。接入时 agent 会在待办里提示你——直接对它说"帮我配一下板块参照"即可。配好后，每天更新时它会从网页自动抓取填入。

**Q：脚本报 SSL/证书错误是怎么回事？**
A：这是公司/校园网的代理拦截了加密连接，**不是股票代码错了**。告诉 agent"加 --insecure 重试"即可（脚本都支持这个参数，仅关闭证书校验，不影响数据内容）。

**Q：日志里一堆"已切换备源"的警告，是坏了吗？**
A：不是。每个字段都有主数据源和备用数据源，主源临时抽风（尤其是东财，偶尔限流）时会自动换备源，数据不受影响。只有同一字段所有源都失败时才会留空并明确告警。

**Q：周末/节假日拉的的数据怎么是空的？**
A：非交易日没有行情。脚本会自动识别，不会写入假数据。

**Q：数据准吗？**
A：每个字段都经过至少两个独立数据源交叉验证（腾讯/东方财富/新浪/搜狐）。你可以随时对 agent 说"检查一下数据完整性"，它会重新核算所有历史数据。

**Q：我想换个股票软件里的字段名，对不上怎么办？**
A：项目里有一份《字段说明表.md》，每个字段的精确口径、数据来源都写在里面，可以让 agent 打开给你看。

## 它不会做什么（安全边界）

- 不会给你荐股、预测涨跌——它只管数据，不管判断
- 不会在数据源挂掉的时候编造数字——拿不到就留空并告诉你
- 不会修改你的历史数据，除非你明确让它修复（且修复前会报告改了什么）

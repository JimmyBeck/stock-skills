# 两个 Skill 的边界契约

本文件是 stock-field-registry（Skill 1）与 stock-data-collector（Skill 2）之间的边界定义。
两个 skill 的 SKILL.md 均引用本文件，不重复定义。**本文件是单一事实源。**

## 核心模型：目录 + 快照

```
Skill 1 产出 → registry.json（目录：字段权威定义，纯知识产物；
                        │        schema 2.0 起含可执行声明：fetcher/params、
                        │        computed.function、checks）
                        │
                        │  仅在"接入/变更"时读取一次，把字段定义
                        │  复制进 per-stock config（完整可执行快照 + layout，带 registry_version 戳）
                        ▼
Skill 2 消费 → config.json（自己持有一切，运行时完全不读 registry；
                        字段执行器按快照定义采集/计算，无需改代码）
```

- **运行时零耦合**：每日自动化只依赖 config 快照，registry 被删了都不影响当天采集
- **接入时耦合 = 一个文件格式**：这是唯一真实的耦合，且是数据耦合不是代码耦合
- **registry 更新不自动传播**：加字段是一次显式的重新接入（人工触发），就像 pip 不会因为发了新版本就自动升级

## 两个正交入口

| 入口 | 触发 | 主语 | 产出 |
|------|------|------|------|
| **加字段** | "我想收集北向资金占比" | 字段 | registry 新条目 |
| **接股票** | "接入 600519，要这些字段" | 股票 | config 快照 + 定时任务 |

两条路独立。同时想做（新股票要新字段）就先走"加字段"再走"接股票"——
因为 Skill 2 接入时要快照的是已经验证过的 registry 条目，字段没准备好就没法快照。

**关键概念**：Skill 1 的主语永远是"字段"，不是"股票"。不存在"新股票过 Skill 1"。
新股票一律从 Skill 2 进入，Skill 2 做能力核查，有缺口才召唤 Skill 1。

## 收敛总规则

> **所有需要"判断、调研、验证"的产物都进 registry（Skill 1 生产）；
> 所有机械执行的状态都在 config（Skill 2 消费）；
> 两者之间只有接入/变更时的一次单向拷贝，其余时间互不知晓。**

## Ready 契约（Skill 1 交给 Skill 2 的门槛）

一个字段必须同时满足以下条件，Skill 2 才能接入：

- [ ] 口径已锁定（名称、单位、计算公式或采集路径，用户已确认）
- [ ] 主信源 + 至少 1 个备用信源已跑通，有 sample_values 证据
- [ ] 已多源交叉验证（cross_validated: true）
- [ ] 更新节奏已确认（available_after: 如 T日16:30）
- [ ] 边界行为已摸清（停牌/除权/涨跌停时该字段返回什么）
- [ ] 适用范围已标注（A股全市场 / 特定行业 / 单只股票）

不满足任一条，Skill 2 有权拒收，不猜测、不补做，直接退回并通知用户。

## 灰色地带归属表

| 场景 | 归属 | 理由 |
|------|------|------|
| 生产环境信源挂了 | Skill 2 先应急（切备源、重试），连续失败→告警→用户→Skill 1 重新调研 | 应急是稳定性职责，根治是信源职责 |
| Skill 1 验证通过但 Skill 2 试跑拿不到 | 退回 Skill 1，附失败样本 | 契约不满足，拒收 |
| 历史数据回填 | Skill 2 执行（复用生产代码），能否回溯/回溯多远由 registry 提供 | 执行归生产，能力定义归调研 |
| 字段语义疑似有变（信源偷偷改口径） | Skill 2 检测（交叉验证偏差告警）→ Skill 1 确认 | Skill 2 有数据看得见异常，Skill 1 有口径能判定 |
| 多字段组合触发限流 | Skill 2 试跑阶段发现→记录→需要时 Skill 1 调研拆源方案 | 只能在组合运行时暴露 |
| registry 格式本身升级 | Skill 1 负责，必须向后兼容或提供迁移说明，Skill 2 按版本协商 | registry 是 Skill 1 的产物 |
| 停牌日的数据行为 | Skill 1 调研写进 registry，Skill 2 按记录执行 | 知识归 1，执行归 2 |
| "这个概念不存在"的引导 | Skill 1，且永远归 Skill 1 | 涉及与用户的语义沟通 |
| 股票代码验证、上市日期、停牌状态 | Skill 2 接入步骤（确定性查询） | 无判断成分，归执行层 |
| 参数个性化（EPS、板块 secid、sheet ID） | Skill 2 config | 改"值"是执行层的事 |
| 方法个性化（该股信源失效、新股无复权） | Skill 1，以 registry override/变体记录 | 改"方法"是知识层的事 |

## 兜底判定规则

> **产出是"知识"还是"数据"？**
> 结论、口径、信源、可行性 = 知识 → Skill 1
> 落库的值、运行状态、告警 = 数据 → Skill 2
>
> 仍无法判定时 = 涉及用户决策 → 停下问用户。

## 反向禁令（防过度耦合）

- **Skill 2 不理解字段语义**：只按 registry 定义机械执行 fetch/计算/校验，遇到语义问题一律上报，绝不自行解释
- **Skill 1 不触碰生产存储**：验证数据只进 registry 的 sample_values，不写正式账本
- **两个 skill 不互相调用**：用户就是总线，传话都经过用户，天然限制耦合失控
